#include <stdio.h>
void main()
{
      int a;
      scanf("%d",&a);
      
	 	
	/*
	
                                 //	if and else
     if(exp) //TEST=> true / false                            
	 {
	 statement(s);
	 }
	*/
	if(a>=0)
	{
	  printf("Positive number");	
	}
	
	// if is independent of else
	// but else is dependent on if statement
}
